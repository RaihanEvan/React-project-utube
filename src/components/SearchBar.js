import React from 'react';
class SearchBar extends React.Component{
    state = {
        searchTerm:'',
    }
    render(){
        return(
            <h1>This is a search component</h1>
        )
    }
}

export default SearchBar;